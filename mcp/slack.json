[
  {
    "name": "send_slack_message",
    "description": "Slack 채널에 메시지 또는 커스텀 블록을 전송합니다. 채널명(#general) 또는 채널 ID(C0BJJFA4344)를 모두 사용할 수 있습니다.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "channel": {
          "type": "string",
          "description": "채널 ID(예: 'C0BJJFA4344') 또는 채널 이름(예: '#general', 'mcp-alerts')"
        },
        "message": {
          "type": "string",
          "description": "전송할 일반 텍스트 메시지"
        },
        "blocks": {
          "type": "array",
          "description": "Slack Block Kit 구조 (선택 사항)"
        }
      },
      "required": ["channel", "message"]
    }
  },
  {
    "name": "list_slack_channels",
    "description": "사용 가능한 Slack 채널 목록(이름 및 채널 ID)을 조회합니다.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "upload_file_to_slack",
    "description": "Slack 채널에 파일(로컬 파일 경로)을 업로드합니다.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "channel": {
          "type": "string",
          "description": "채널 ID 또는 채널 이름"
        },
        "file_path": {
          "type": "string",
          "description": "업로드할 로컬 파일 경로 (예: 'README.md')"
        },
        "comment": {
          "type": "string",
          "description": "파일과 함께 게시할 코멘트 메시지 (선택 사항)"
        }
      },
      "required": ["channel", "file_path"]
    }
  }
]
